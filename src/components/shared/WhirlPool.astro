<div class="fixed w-screen h-screen flex justify-center items-center -z-10">
  <div class="bg-whirlpool"></div>
</div>

<style>
  .bg-whirlpool {
    --size: 80dvw;
    --speed: 25s;
    --easing: cubic-bezier(0.8, 0.2, 0.2, 0.8);

    width: var(--size);
    height: var(--size);
    filter: blur(calc(var(--size) / 5));
    background-image: linear-gradient(rgb(55, 235, 169), rgb(92, 56, 235));
    animation: rotate var(--speed) var(--easing) alternate infinite;
    border-radius: 30% 70% 70% 30% / 30% 30% 70% 70%;
    opacity: 0.4;
  }

  @media (min-width: 720px) {
    .bg-whirlpool {
      --size: 50dvw;
    }
  }

  @keyframes rotate {
    0% {
      transform: rotate(0deg);
    }
    100% {
      transform: rotate(360deg);
    }
  }
</style>
