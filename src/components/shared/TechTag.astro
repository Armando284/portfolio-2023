---
interface Props {
  tags?: string[]
}

const { tags = [] } = Astro.props
---

<div class="w-full flex flex-wrap gap-2">
  {
    tags.map((tag) => (
      <span class="bg-purple-700 text-purple-100 px-2 rounded-md capitalize text-sm shadow-md">
        {tag}
      </span>
    ))
  }
</div>
