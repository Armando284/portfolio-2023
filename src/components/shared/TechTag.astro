---
interface Props {
  tags?: string[]
}

const { tags = [] } = Astro.props
---

<div class="w-full flex flex-wrap gap-2">
  {
    tags.map((tag) => (
      <span class="bg-yellow-300 px-2 italic rounded-md capitalize text-sm shadow-md">
        {tag}
      </span>
    ))
  }
</div>
