---
interface Props {
  url: string
  description: string
  style?: 'elevated' | 'flat'
}

const { url, description, style = 'elevated' } = Astro.props
---

<a
  class:list={[
    'flex items-center justify-center hover:scale-110 transition-transform rounded-md text-slate-100',
    {
      'w-10 h-10 shadow-purple-900 shadow-md bg-purple-600 hover:bg-purple-500':
        style === 'elevated',
      'w-7 h-7 text-slate-600': style === 'flat',
    },
  ]}
  href={`https://${url}`}
  aria-label={description}
  target="_blank">
  <slot />
</a>
